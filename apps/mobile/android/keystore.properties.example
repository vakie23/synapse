# Copy to keystore.properties and fill in after you create a release keystore.
# Do NOT commit keystore.properties or your .jks file to git.

storeFile=../release/synapse-release.jks
storePassword=your-store-password
keyAlias=synapse
keyPassword=your-key-password
